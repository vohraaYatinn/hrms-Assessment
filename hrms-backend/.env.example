SECRET_KEY=
DEBUG=True
DATABASE_URL=
# Comma-separated hostnames only (no https://). On Render, RENDER_EXTERNAL_HOSTNAME is added automatically in production.
ALLOWED_HOSTS=example.com,www.example.com
CORS_ALLOWED_ORIGINS=https://hrms-assessment-five.vercel.app
# Production on Render: optional extra origins (comma-separated). All *.vercel.app HTTPS
# origins are allowed automatically in production.py for CORS preflight.
# If you use session cookies / CSRF across origins, set this to your frontend origin(s):
CSRF_TRUSTED_ORIGINS=https://hrms-assessment-five.vercel.app
